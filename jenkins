pipeline {
  agent {
      node {
          label "ubu01"
          
      }

  }

stages {
    stage("build") {
        steps {
            sh "mvn -version"
            sh "mvn clean install"
        }
    }
}

post {
    always {
        cleanWs()
    }
}
}
