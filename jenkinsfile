
pipeline {
    environment {
        JAVA_TOOL_OPTIONS = "-Duser.home=/home/tonymac"
    }
    agent {
        dockerfile {
            label "docker"
            args "-v /tmp/maven:/home/tonymac/.m2 -e MAVEN_CONFIG=/home/tonymac/.m2"
        }
    }

    stages {
        stage("Build") {
            steps {
                sh "ssh -V"
                sh "mvn -version"
                sh "mvn clean install"
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}